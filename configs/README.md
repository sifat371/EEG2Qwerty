# Configurations

Configurations in this directory describe **public benchmark candidates** and reproducibility metadata. A config does not imply that the corresponding result has already been validated.

Historical experiment scripts retain their original command-line arguments under `experiments/historical/`.

The standardized framework uses explicit protocol fields so that target definition, sentence batching, and evaluation mode are never implicit.
